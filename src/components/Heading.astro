---
type Props = {
  title: string;
};

const { title } = Astro.props;
---

<header class="flex flex-col items-center sm:mt-24">
  <h1 class="mb-6 text-center font-playfair leading-tight">{title}</h1>
</header>
